{
  "name": "websocket-test",
  "version": "1.0.0",
  "description": "A simple WebSocket API that receives the text (code) whenever the user types something in the editor.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "websocket"
  ],
  "author": "j",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.12.1"
  }
}
